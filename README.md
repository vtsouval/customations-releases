# Customations releases

Public signed Android installers, Windows installers and update manifests. Application source and credentials remain in the private development repository.

GameHub automatic-update feed: `gamehub/manifest.json`. Versioned binaries are attached to GameHub releases.
